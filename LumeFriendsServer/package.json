{
  "name": "lume-friends-server",
  "version": "0.1.0",
  "description": "Presence + friends + shared-waypoint backend for Lume Client (online status, add-friend, auto-tpa data, cross-server markers)",
  "private": true,
  "main": "src/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
